events {}
http {
  server {
    listen 8080;
    location = /internal/control { return 404; }
    location / { proxy_pass http://order-api:8080; proxy_connect_timeout 1s; proxy_read_timeout 3s; }
  }
}
